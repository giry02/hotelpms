# Hotel PMS 사용자 테스트 / 통합 테스트 가이드

작성일: 2026-06-25  
목적: 반복 발생한 테스트 누락을 기록하고, 이후 모든 화면 수정 후 동일 기준으로 사용자 테스트, 통합 테스트, 데이터 적합성 테스트를 수행하기 위한 기준 문서입니다.

## 1. 반복 실수 기록

아래 항목은 실제 작업 중 반복적으로 놓친 내용입니다. 이후 검수 시 우선순위를 가장 높게 둡니다.

| 구분 | 놓친 내용 | 영향 | 앞으로의 검증 기준 |
| --- | --- | --- | --- |
| 날짜 기준 | 오늘, 금일, 주간, 월간 기준이 화면마다 다르게 적용됨 | 시연일 기준 데이터가 맞지 않아 신뢰 저하 | 모든 화면은 `PmsDate.today()` 또는 동일 영업일 기준을 사용하고, 금일 필터 결과가 실제 기준일과 일치해야 함 |
| 타임라인 상태 | 미래 숙박 구간까지 `투숙 중`으로 표시됨 | 아직 도착하지 않은 날짜가 재실처럼 보임 | 투숙 중은 실제 오늘 점유 구간만 표시하고, 미래 구간은 예약/배정 상태로 분리 |
| 색상 의미 | 회색 블록 의미가 모호함 | 사용자가 상태 기준을 해석할 수 없음 | 범례와 실제 상태명이 정확히 대응해야 하며, 회색은 `체크아웃 완료 / 점검`처럼 명확히 표시 |
| 객실 정렬 | 객실 목록이 데이터 배열 순서에 의존함 | 객실이 무작위처럼 보여 운영자가 찾기 어려움 | 건물, 층, 호실 기준의 명시적 정렬 함수를 사용하고 첫 화면에서 순서를 검증 |
| 다국어 | 한국어 화면에 영어 키, 미번역 라벨, 데이터 번역이 섞임 | 고객 시연 품질 저하 | UI 라벨은 i18n 처리, 고객/업체가 입력한 데이터는 임의 번역 금지 |
| 하드코딩 데이터 | 화면 내 임의 텍스트/금액/상태가 데이터와 불일치 | 화면 간 데이터 신뢰성 저하 | 화면 표시 값은 mock API 또는 공통 데이터 소스에서 계산해야 함 |
| 정산 데이터 | 정산 목록, 매출 분석, 일일 마감 금액이 서로 다름 | 운영 기준을 설명하기 어려움 | 동일 Revenue/Folio/Settlement 소스를 기준으로 합계, 미수금, 예치금, 결제수단을 대조 |
| 필터/정렬 | 필터를 누른 뒤 다른 정렬 조건이 남아 있거나 해제되지 않음 | 사용자가 결과를 오해함 | 필터, 정렬, 기간 선택은 상호 상태 초기화 규칙을 검증 |
| 팝업/상세 | 목록의 정보와 팝업 상세 정보가 다르거나 부족함 | 운영자가 실제 처리 판단을 못함 | 목록 클릭, 상세 버튼, 팝업의 데이터가 같은 엔티티를 가리키는지 확인 |
| 권한/감사 로그 | 개인정보 열람, 다운로드, 관리자 기능 접근이 로그에 일관되게 남지 않음 | 보안/감사 기준 미흡 | 연락처, 이메일, 특이사항, 엑셀 다운로드 등 개인정보 접근은 감사 로그 기록 확인 |
| 레이아웃 | 버튼/검색/정렬/테이블/팝업이 화면마다 다름 | 완성도가 낮아 보임 | 동일 계열 화면은 검색 영역, 필터, 버튼 위치, 테이블 간격을 기준 화면과 비교 |
| 샘플 데이터 | 시연에 필요한 케이스가 부족함 | 버튼/필터/상태를 확인할 수 없음 | 완료/미완료/미수금/블랙리스트/레이트 체크아웃/변경 이력 등 필수 케이스 포함 |

## 2. 테스트 원칙

1. 수정한 화면만 보지 않고 연관 화면까지 확인합니다.
2. 데이터는 화면에 “보이는지”가 아니라 업무적으로 “말이 되는지”를 검증합니다.
3. 오늘, 금일, 체크인, 체크아웃, 마감일은 항상 실제 영업일 기준으로 검증합니다.
4. 호텔 운영자가 20년 경력자라고 가정하고, 버튼명과 상태명이 오해 없이 읽히는지 확인합니다.
5. 고객이 입력한 데이터와 시스템 라벨을 구분합니다. 객실 유형명, 고객명, 업체명은 임의 번역하지 않습니다.
6. GitHub Pages에서 확인하는 것을 최종 기준으로 둡니다. 로컬 통과만으로 완료하지 않습니다.

## 3. 수정 후 필수 실행 순서

### 3.1 정적 검증

```powershell
git diff --check
npm run audit:data
npm run audit:storage
```

확인 기준:

- JSON 파싱 오류가 없어야 합니다.
- 화면 데이터가 서로 다른 mock source에서 불일치하지 않아야 합니다.
- localStorage에만 의존해 새로고침 후 사라지는 핵심 데이터가 없어야 합니다.

### 3.2 페이지 스모크 테스트

```powershell
npm run smoke
npm run smoke:all
```

확인 기준:

- 주요 페이지가 열립니다.
- 콘솔 오류가 없어야 합니다.
- 한국어/영어 언어 전환 후 키 문자열이 그대로 노출되지 않아야 합니다.
- 검색, 필터, 정렬, 페이지네이션 기본 동작이 끊기지 않아야 합니다.

### 3.3 상호작용 테스트

```powershell
npm run smoke:interactions
npm run e2e
npm run test:reservations
npm run test:groups
```

확인 기준:

- 목록 클릭 시 상세/관리 팝업이 열립니다.
- 버튼 클릭 후 상태 변경이 화면과 데이터에 함께 반영됩니다.
- 예약, 단체, 정산, 하우스키핑, 시설보수처럼 연결된 업무 흐름은 다음 화면에 결과가 이어져야 합니다.

### 3.4 시각 레이아웃 테스트

```powershell
npm run audit:visual
```

확인 기준:

- 검색 영역, 필터, 정렬 버튼 간격이 기준 화면과 일관됩니다.
- 테이블이 가로 스크롤을 만들지 않습니다. 단, 명시적으로 필요한 대형 표는 예외입니다.
- 팝업 제목, 버튼, 하단 액션 영역이 깨지지 않습니다.
- 텍스트가 버튼이나 카드 밖으로 넘치지 않습니다.

### 3.5 GitHub Pages 최종 검증

수정 후 반드시 커밋과 푸시를 먼저 수행합니다.

```powershell
git status --short
git add <changed-files>
git commit -m "<message>"
git push origin main
git ls-remote origin main
```

그 다음 GitHub Pages URL에 cache busting 값을 붙여 확인합니다.

```text
https://giry02.github.io/hotelpms/<page-path>?cb=<commit-hash>
```

확인 기준:

- raw GitHub와 GitHub Pages가 모두 새 커밋 내용을 포함해야 합니다.
- 브라우저 localStorage 캐시 때문에 예전 데이터가 보이는 경우 API/mock data 버전을 갱신하거나 초기화 루틴을 확인합니다.

## 4. 업무 영역별 필수 검수 항목

### 4.1 예약 타임라인 / 예약 관리

- 객실 정렬 기준이 명확한지 확인합니다.
  - 기본 기준: 건물 → 층 높은 순 → 호실 오름차순.
- 오늘 날짜 표시가 실제 오늘 날짜에만 붙는지 확인합니다.
- 1박 2일 예약이 최소 2칸 이상으로 보이는지 확인합니다.
- 체크인 전 미래 날짜가 `투숙 중`으로 표시되지 않는지 확인합니다.
- 체크인, 체크아웃, 취소, 노쇼 상태가 예약 목록과 타임라인에서 일치하는지 확인합니다.
- 단체 예약으로 생성된 객실/투숙객이 단체 관리와 예약 관리에서 동일하게 이어지는지 확인합니다.
- 예약현황은 단체 필터를 제공해야 하며, 객실 검색에서 단체명/업체명으로도 단체 객실이 검색되어야 합니다.
- 예약 목록 검색도 단체명, 업체명, 단체 ID로 검색되어야 하며 예약현황과 검색 기준이 일치해야 합니다.
- 예약현황 카드도 예약 타임라인과 동일하게 단체 배지, 단체명, VIP 표시를 제공해야 하며 고객명이 단체명에 묻히면 안 됩니다.

### 4.2 단체/행사 관리

- 행사 상태가 서로 배타적인지, 또는 복수 상태를 동시에 가져야 하는지 업무적으로 검토합니다.
  - 예: 지난 행사이면서 정산 필요인 경우 두 조건 모두 필터에 반영되어야 함.
- 업체는 업체 관리 데이터에서 선택되어야 하며, 행사 화면에서 임의 미지정 데이터가 나오면 안 됩니다.
- 행사 담당 영업 직원과 업체 담당자를 혼동하지 않도록 라벨을 확인합니다.
- 객실 배정, 투숙객 등록, 변경 이력이 비어 있지 않은 시연 데이터를 포함합니다.

### 4.3 객실 / 요금 관리

- 객실 유형은 고객이 관리하는 데이터이므로 임의 번역하지 않습니다.
- 객실 유형 정렬은 객실명/구분이 함께 묶이도록 정렬합니다.
- 요금 캘린더와 일괄 수정의 객실 유형 순서가 동일해야 합니다.
- 월 변경, 장기 예약, 미래 요금 등록이 동작해야 합니다.

### 4.4 하우스키핑 / 시설보수

- 하우스키핑 KPI와 필터 카운트가 카드 수와 일치해야 합니다.
- 재실 청소 요청, 청소 시작, 청소 완료 상태가 카드와 필터에서 같은 기준으로 계산되어야 합니다.
- 예약현황/하우스키핑 카드의 방해금지 상태는 금지 아이콘만 보이지 않고 `방해금지`로 명확히 표시되어야 하며, 요약 badge가 `청소 필요`와 혼동되면 안 됩니다.
- 부가서비스 등록은 실제 `투숙 중` 상태의 객실에서만 가능해야 하며, 체크인 예정/공실 객실은 등록 팝업이 열리지 않고 안내 메시지만 표시되어야 합니다.
- 부가서비스 금액과 이용 내역은 실제 `투숙 중` 객실 기준으로만 표시하며, 체크인 예정/공실 객실에 사전 이용금액처럼 보이면 안 됩니다.
- 시설보수는 호실 직접 입력이 아니라 기존 객실 선택이어야 합니다.
- 티켓 발행 일시, 작업 시작 일시, 완료 확인 일시가 구분되어야 합니다.
- `완료` 버튼은 실제 완료인지, `완료 확인`인지 업무 의미를 확인합니다.

### 4.5 매출 분석 / 정산 목록 / 일일 마감

- 매출 분석과 정산 목록은 같은 매출 요약 기준을 사용해야 합니다.
- 정산 기준은 체크아웃입니다.
- 예치금은 매출이 아니라 보관금액으로 분리합니다.
- 미수금, 수납 완료, 완료 대기, 정산 완료 상태가 필터와 명세서에 일치해야 합니다.
- 결제수단별 카드/현금/기타 구분이 정산 흐름에 표시되어야 합니다.
- 일일 마감은 마감 체크와 시재 인수인계를 혼동하지 않아야 합니다.
- 시재는 현금 흐름만 다루고, 정산 검증은 마감 체크에서 다룹니다.

### 4.6 부가서비스

- 통합 POS, 골프, 렌터카는 주문/예약/정산 상태가 일관되어야 합니다.
- 업체와 카테고리는 고객이 관리하는 데이터인지, 시스템 코드인지 구분합니다.
- 완료 전 수정/삭제 가능 여부를 확인합니다.
- 객실 선택 시 고객명이 자동으로 연결되는지 확인합니다.
- 객실 카드는 통합 POS, 골프장, 렌터카, 합산 금액만 요약하고 세부 항목은 상세 팝업에서 확인해야 합니다.
- 바우처 출력 양식은 업체/항목 관리에서만 설정하고, 부가서비스 등록 팝업 안에서는 출력 필드 체크박스를 다시 노출하지 않습니다.
- 기존 이용 내역이 있는 객실은 상세 팝업을 먼저 열고, `추가 등록`으로 등록 흐름에 들어가야 합니다. 이용 내역이 없으면 등록 팝업을 바로 열어도 됩니다.
- 상세 팝업에서 등록, 수락, 완료, 취소 상태가 구분되고 골프장/렌터카는 등록된 주문 기준으로 바우처 출력이 가능해야 합니다.

### 4.7 투숙객 CRM / 멤버십

- 연락처, 이메일, 특이사항은 목록에 과다 노출하지 않고 상세 열람 시 감사 로그를 남깁니다.
- 등급 기준과 등급 변동 이력은 별도 메뉴로 접근 가능해야 합니다.
- 블랙리스트, 수동 등급 변경, 자동 승급 케이스가 데이터에 포함되어야 합니다.
- 고객이 입력한 국적 값은 임의 번역하지 않고 기준 목록 중복을 제거합니다.

### 4.8 직원 / 권한 / 감사 로그

- admin은 한 명만 유지합니다.
- admin 정보는 일반 직원 편집 화면에서 임의 변경하지 않습니다.
- 역할 색상은 중복 선택되지 않아야 합니다.
- 개인정보 열람, 엑셀 다운로드, 예약 변경, 정산 처리, 객실 이동은 감사 로그에 기록되어야 합니다.
- 감사 로그는 기간 필터, 유형 필터, 페이지네이션이 동작해야 합니다.

### 4.9 관리자 화면

- 호텔 대시보드에서 만든 기능이 관리자 화면에도 필요한지 매번 대조합니다.
- 호텔과 관리자 매뉴얼은 분리합니다.
- 관리자 화면의 검색, 정렬, 팝업, 상태 배지는 호텔 화면 기준과 시각적으로 일관되어야 합니다.

## 5. 데이터 적합성 테스트 체크리스트

수정 후 다음 질문에 모두 답해야 합니다.

- 이 값은 사용자가 입력한 데이터인가, 시스템 라벨인가?
- 이 값은 mock API에서 오는가, 화면에 하드코딩되어 있는가?
- 같은 데이터가 다른 화면에서도 같은 금액/상태/일자로 보이는가?
- 오늘 기준 데이터와 미래 데이터가 섞여 오해를 만들지 않는가?
- 완료/미완료/오류/미수금/블랙리스트처럼 예외 케이스가 하나 이상 존재하는가?
- 팝업에서 수정한 값이 목록, KPI, 관련 로그에 반영되는가?
- GitHub Pages에서 localStorage 초기 상태로도 동일하게 재현되는가?

## 6. 매번 남길 검수 리포트 형식

앞으로 완료 보고 시 아래 형식을 사용합니다.

```text
검수 기준:
- 실행한 자동 테스트:
- 직접 확인한 화면:
- 확인한 데이터 케이스:
- 발견한 문제:
- 수정한 내용:
- 남은 리스크:
- GitHub Pages 확인 URL:
- 커밋:
```

## 7. 완료 기준

작업 완료는 다음 조건을 모두 만족할 때만 말합니다.

1. 관련 파일 수정 완료.
2. 자동 테스트 또는 해당 범위에 맞는 Playwright 직접 검증 완료.
3. GitHub Pages에서 새 커밋 기준 확인 완료.
4. 사용자에게 데이터 기준, 상태 기준, 남은 리스크를 보고.
5. 커밋 및 푸시 완료.

## 8. 사용자 지적사항 누적 규칙

사용자가 화면 오류, 데이터 불일치, 업무 로직 문제, 다국어 혼용, 레이아웃 깨짐을 지적하면 해당 내용은 일회성 수정으로 끝내지 않고 이 문서의 회귀 테스트 케이스에 추가합니다.

운영 규칙:

1. 사용자가 지적한 문제는 `지적 내용`, `원인`, `재발 방지 검증 방법`, `확인 화면`으로 기록합니다.
2. 같은 유형의 문제가 다른 화면에도 있을 가능성을 보고 관련 화면을 함께 검수합니다.
3. 수정 후에는 로컬 테스트뿐 아니라 GitHub Pages 기준으로 재현 여부를 확인합니다.
4. 자동화가 가능한 항목은 Playwright 또는 기존 mock-api 테스트 스크립트에 반영합니다.
5. 자동화가 어려운 항목은 완료 보고 시 수동 검수 결과를 남깁니다.

## 9. 사용자 지적 회귀 테스트 케이스

| ID | 지적 내용 | 원인 | 검증 방법 | 상태 |
| --- | --- | --- | --- | --- |
| UAT-REG-001 | 예약 타임라인 객실 정렬 기준이 불명확함 | 객실 배열 순서를 그대로 사용하거나 정렬 기준 설명이 없음 | 객실 순서가 건물 → 층 높은 순 → 호실 오름차순으로 표시되는지 확인 | 등록 |
| UAT-REG-002 | 예약 타임라인 회색 블록 기준이 불명확함 | 회색 범례가 `이전 예약 / 점검`처럼 모호함 | 범례와 블록 상태가 `체크아웃 완료 / 점검`으로 명확히 보이는지 확인 | 등록 |
| UAT-REG-003 | 아직 오지 않은 날짜가 `투숙 중`으로 표시됨 | 체크인 상태 예약 전체 기간을 초록색으로 렌더링함 | 오늘 칸만 초록색이고 미래 숙박 구간은 예약/배정 상태로 분리되는지 확인 | 등록 |
| UAT-REG-004 | 오늘 표시가 표시 범위 첫 칸처럼 보일 수 있음 | 날짜 헤더에서 `d === 0`을 오늘 기준으로 사용함 | 실제 오늘 날짜와 같은 칸에만 today class가 붙는지 확인 | 등록 |
| UAT-REG-009 | 체크아웃 전 Folio가 정산 목록과 KPI에 표시됨 | 정산 목록에서 API Folio의 `open` 상태와 `closedAt` 없는 데이터를 함께 렌더링함 | 정산 목록 금일/주간/월간 조회 시 체크아웃 일자가 `-`인 행이 없어야 하며, 미수금 샘플도 `closedAt`이 있는 체크아웃 완료 데이터로만 표시되는지 확인 | 수정/자동검증 |
| UAT-REG-010 | 체크아웃 전 수납금 용어가 `선결제`와 `예치금`으로 혼용됨 | 예약 팝업, 예약 목록, 객실 투숙정보, 감사 로그, 마감 샘플 문구가 서로 다른 용어를 사용함 | 한국어 화면에서 체크아웃 전 수납금은 모두 `예치금`으로 표시되고, `선결제` 문구가 보이지 않는지 확인 | 수정/자동검증 |
| UAT-REG-011 | 미래 예약 상세에서 체크인이 노출되고 클릭 시 잘못된 `이미 투숙 중` 메시지가 표시됨 | 예약 상세 팝업의 체크인 버튼 노출 조건과 실제 체크인 실행 조건이 분리됨 | 입실 예정일이 아직 도래하지 않은 예약 상세에서는 `체크인 처리` 버튼이 보이지 않고 `예약 취소`는 표시되어야 하며, 강제 호출 시에도 `체크인 예정일 전에는 체크인할 수 없습니다` 안내가 나오는지 확인 | 등록 |
| UAT-REG-012 | 예약현황 카드 색상이 청소 상태 중심으로 보여 예약/점유 상태를 한눈에 구분하기 어려움 | 청소 상태 클래스가 카드 전체 테두리와 배경을 덮어 예약 상태 색상보다 우선 표시됨 | 예약현황 카드 주색은 체크인 예정/투숙 중/체크아웃 예정/예약 예정/예약 없음/단체 배정/점검 중 기준으로 보이고, 청소 상태는 카드 하단 배지와 풀다운으로만 변경되는지 확인 | 수정/수동검증 |
| UAT-REG-013 | 삭제하기로 한 객실 목록 메뉴가 다시 노출됨 | 객실 관리 부모 메뉴와 대시보드 링크가 `operations/rooms.html`로 남아 있음 | 사이드바 객실 관리 하위 메뉴에 `객실 목록/Room List`가 없고, 대시보드 객실 현황 링크가 예약현황으로 이동하는지 확인 | 수정/자동검증 |
| UAT-REG-014 | 예약현황 단체 카드가 단체 아이콘만 보이고 단체명이 보이지 않음 | 예약현황이 단체 목록을 로드하지 않고 `groupName`이 없으면 `단체 블록`으로만 표시함 | 단체 예약/블록 카드에 `단체명 · 투숙객명` 또는 `단체명 · 투숙객 미배정`이 표시되고, 단체명 검색 시 해당 카드만 남는지 확인 | 수정/자동검증 |
| UAT-REG-015 | 예약현황/예약목록 청소 상태 버튼이 5개라 재실/외출까지 섞여 보임 | 청소 업무 화면에 투숙 상태성 버튼을 함께 배치함 | 예약현황과 예약목록의 배정 객실 청소 상태 풀다운은 `방해금지`, `청소필요`, `청소완료` 3개만 표시되고 `재실`, `외출`, `청소 요청 없음` 옵션은 없어야 함 | 수정/자동검증 |
| UAT-REG-016 | 투숙중 객실의 전일 청소완료/DND 상태가 다음 영업일에도 유지될 수 있음 | 재실 청소 상태를 날짜 없이 객실 상태값으로만 저장함 | 투숙중/체크아웃 대상 객실은 매 영업일 첫 진입 시 `청소 필요`로 갱신되고, 빈방은 전일 `청소완료/청소필요` 상태가 그대로 유지되는지 확인 | 수정/자동검증 |
| UAT-REG-017 | 청소 상태 배지에 `재실 중`처럼 투숙 상태가 섞여 표시됨 | 청소 요약 함수가 청소 상태가 없을 때 예약 존재 여부를 기준으로 `재실 중`을 반환함 | 예약현황의 청소 배지는 `청소 완료`, `청소 필요` 두 값만 표시되고, 투숙 여부는 예약 상태 영역에서만 확인되는지 검증 | 등록 |
| UAT-REG-024 | 일일 마감 시재 인수인계의 전일 마감현금 카드에서 통화 목록이 타이틀 위치부터 시작해 왼쪽 여백이 과하게 보임 | 카드가 아이콘과 본문을 좌우 flex로 배치해 통화 라인도 본문 컬럼 안에서 시작함 | 전일 마감현금 카드의 아이콘은 타이틀 높이에 맞고, 페소/달러/원화 라인은 헤더 아래에서 같은 왼쪽 기준으로 시작하는지 확인 | 수정/수동검증 |
| UAT-REG-025 | 예약현황의 상태명 `예약 중`, `배정된 예약 없음`이 체크인/숙박/빈 객실 의미를 흐림 | 영업일 기준 상태와 단순 예약 존재 여부 문구가 섞여 표시됨 | 상태명은 `체크인 예정`, `투숙 중`, `체크아웃 예정`, `예약 예정`, `예약 없음`, `단체 배정`, `점검 중`처럼 업무 의미가 분명해야 하며 검색 옆 색상 범례와 일치해야 함 | 수정/수동검증 |
| UAT-REG-026 | 예약 타임라인 최초 진입일이 오늘이 아니라 단체 블록 시작일로 이동하고 토/일이 모두 빨간색으로 표시됨 | 초기 렌더링 후 단체 블록이 보이도록 `baseDate`를 다시 덮고, 주말을 하나의 `weekend` 클래스로 처리함 | 최초 진입 시 첫 날짜는 실제 오늘이어야 하고, 좌우 이동은 7일 단위로만 변경되어야 하며, 토요일은 파랑/일요일은 빨강으로 구분되는지 확인 | 수정/자동검증 |
| UAT-REG-027 | 예약 생성/수정 팝업의 총 객실 금액과 추후 정산 잔액이 입력칸처럼 보여 수동 입력값으로 오해됨 | 1박 단가와 숙박일수로 계산되는 값까지 input 형태로 렌더링함 | `총 객실 금액`과 `추후 정산 잔액`은 입력 박스가 아니라 계산 표시 텍스트로 보이고, 저장값은 `1박 객실 단가 × 숙박일수 - 예치금` 기준으로 유지되는지 확인 | 수정/자동검증 |
| UAT-REG-028 | 단체 등록 화면이 `업체 등록/업체명`으로 표시되어 일반인이 단체 대표가 되는 케이스를 설명하지 못함 | 단체 등록 데이터가 회사/여행사 중심의 업체 용어로 노출됨 | 단체 등록 관리 화면과 행사 상세 연결 영역은 `신규 단체 등록`, `단체명`, `대표 유형`, `등록 단체`로 표시되고, 대표 유형에 `일반인 대표`가 유지되는지 확인 | 수정/자동검증 |
| UAT-REG-029 | 등급 기준 요약이 `20회 이상`, `₱10,000+`처럼 나열되어 투숙/결제 조건이 AND인지 OR인지 헷갈림 | 기준 표시가 두 조건 사이의 관계를 설명하지 않고 `+` 기호로만 표현함 | 등급 기준 요약과 모달은 `투숙 N회 이상 또는 누적 결제 ₱X 이상`, `둘 중 하나 충족`으로 표시되고 일반 등급에는 `0회 이상/₱0+` 대신 `가입 즉시 기본 등급`이 표시되는지 확인 | 수정/자동검증 |
| UAT-REG-030 | 대시보드 광고 위치가 변경되면 메인 화면 의도와 사용자 확인 흐름이 달라짐 | 처리 업무 영역을 추가하면서 광고 캐러셀보다 위에 배치하면 기존 첫 화면 구성이 바뀜 | 대시보드 최상단은 광고 캐러셀과 제휴 이벤트 영역을 유지하고, `오늘 처리 업무`는 KPI와 주간 객실 현황 아래에 표시되는지 확인 | 등록 |
| UAT-REG-031 | 정산 목록은 전체 이력이라 오늘 처리할 정산 대상이 직관적으로 보이지 않음 | 정산 이력 조회와 당일 처리 큐가 한 화면 안에서 같은 목록 형태로 섞임 | 별도 메뉴 `정산 현황`은 오늘 처리 가능한 미수금/정산 필요/예치금/단체 Folio를 카드형으로 보여주고, `정산 목록`은 전체 이력과 상세 수납 화면으로 유지되는지 확인 | 등록 |
| UAT-REG-032 | 예약현황에서 청소/점검/정산 가능 여부를 한 카드에서 판단하기 어려움 | 숙박 상태, 청소 상태, 정산 상태가 서로 다른 위치 기준 없이 섞여 보임 | 예약현황 카드는 주색으로 숙박 상태를 표시하고, 하단에는 청소 상태 드롭다운과 `체크인 가능/청소 확인 필요`, `정산 필요/미수금 있음/예치금 있음/정산 완료` 배지가 별도 줄로 표시되는지 확인 | 등록 |
| UAT-REG-033 | 우측 상단 알림이 시간 기반 업무 재촉과 운영 이벤트 로그 사이에서 의미가 불명확함 | 알림 데이터가 샘플 점검/활동 기록 중심이라 누가 무엇을 공유해야 하는지 구분되지 않음 | 알림은 객실 변경, POS/렌터카/골프 등록, 단체 명단 변경, 결제/환불 승인, 객실 상태 변경처럼 공유해야 할 행위 이벤트로 표시되고, 각 항목은 대상 화면으로 이동하며 동일 내용이 알림 로그에 남는지 확인 | 등록 |
| UAT-REG-034 | 정산현황 카드에 체크아웃 일자만 표시되어 몇 박 며칠 정산인지 판단하기 어려움 | Folio 기준일만 표시하고 연결 예약/단체의 체크인·체크아웃 기간을 계산하지 않음 | 정산현황 카드에는 `투숙 6/29~7/3 · 4박 5일`처럼 투숙 기간과 박수/일수가 표시되고, 예약 연결이 없는 Folio는 정산 기준일로 `1박 2일 추정`이 표시되며, 체크아웃 일자는 별도 줄로 유지되는지 확인 | 등록 |
| UAT-REG-035 | 대시보드가 오늘 필요한 업무를 한눈에 보고 세부 화면으로 이동하는 화면처럼 보이지 않음 | 메뉴 바로가기나 선택형 업무 큐가 중복되어 실제 처리 대상과 세부 목록을 동시에 보기 어려움 | 대시보드는 광고/주간 객실 현황 아래에 운영 KPI 4개(`오늘 체크인`, `정산 필요`, `청소 필요`, `새 알림`)를 먼저 보여야 하며, KPI는 상단 객실 KPI와 중복되어 보이지 않도록 컴팩트해야 합니다. 그 아래에는 `오늘 체크인 예정` 테이블, `실시간 활동`, `오늘 부가서비스 주문`, `부가서비스 매출`, `하우스키핑 현황`이 동시에 표시되어야 합니다. 체크인 테이블은 예약 목록처럼 고객/객실/유형/숙박/상태/액션을 한 줄로 보여주고 기본 5건 이후는 `더보기/접기`로 전환해야 하며, KPI의 오늘 체크인 건수와 기본 표시 건수가 불일치해 보이지 않아야 합니다. 테이블 본문은 과한 볼드 없이 예약 목록과 비슷한 굵기로 보이고, 개인/단체 구분과 VIP/등급을 표시해야 합니다. 체크인 액션은 예약현황으로 이동해 해당 객실 기준으로 확인할 수 있어야 하고, 정산 필요는 오늘 기준 대상만 표시해 오래된 누적 미수금이 대시보드 총액처럼 보이지 않아야 합니다. | 등록 |

| UAT-REG-036 | 정산현황 검색영역에서 검색 버튼이 입력창 안으로 끼어 들어가 검색창이 깨져 보임 | 공통 i18n 검색 버튼 자동 삽입 로직이 `settlement-search` 컨테이너를 인식하지 못하고 input 바로 뒤에 버튼을 삽입함 | 정산현황 검색영역은 `검색창 + 검색 버튼`이 독립 칸으로 같은 줄에 배치되고, 좁은 화면에서는 필터 칩만 다음 줄로 내려가며 입력창 텍스트와 버튼이 겹치지 않는지 확인 | 등록 |
| UAT-REG-037 | 정산현황 상단의 `정산 목록`, `일일 마감` 이동 버튼이 너무 작아 잘 보이지 않음 | 공통 소형 보조 버튼 스타일을 핵심 이동 버튼에 그대로 사용함 | 정산현황 상단 이동 버튼은 40px 이상 높이와 충분한 좌우 여백을 갖고, 아이콘/라벨이 쉽게 읽히며 모바일에서도 겹치지 않는지 확인 | 등록 |
| UAT-REG-038 | 정산현황 `처리 대상`에 오래된 미수금이 모두 섞여 오늘 체크아웃 정산 건을 찾기 어려움 | 기본 목록이 상태가 `미수금/정산 필요/환불`이면 체크아웃일과 무관하게 모두 포함함 | `처리 대상`은 금일 체크아웃일인 Folio 중 정산/수납/환불 확인이 필요한 건만 표시하고, 오래된 미수금은 `미수금` 필터에서만 확인되며, `정산 필요`/`예치금`/`단체` 필터도 금일 체크아웃 기준으로 좁혀지는지 확인 | 등록 |
| UAT-REG-039 | 정산현황에서 `수납`을 누르면 정산목록으로 이동해 금일 체크아웃 정산을 한 화면에서 끝내기 어려움 | 정산현황 카드의 `수납/상세` 액션이 정산목록 URL로 이동하고, 상세 팝업의 정산 완료 버튼도 수납 이후 흐름과 분리되어 보임 | 정산현황 카드의 `수납 등록/상세`는 같은 화면의 정산 상세 팝업을 열고, 팝업 하단은 좌측 `닫기/인쇄`, 우측 `수납 등록/정산 완료`로 배치되며, 수납 후 같은 상세 팝업으로 돌아와 `정산 완료`를 누르면 `checkoutCompleted: true`와 `roomNextStatus: dirty`가 저장되는지 확인 | 등록 |
| UAT-REG-040 | 통합 정산 하위 메뉴가 매출 분석을 먼저 보여 금일 정산 업무보다 리포트가 앞서 보임 | 정산 현황 신설 이후에도 기존 리포트 우선 메뉴 순서가 유지됨 | 통합 정산을 펼치면 `정산 현황` → `정산 목록` → `일일 마감` → `매출 분석` 순서로 보이고, 상위 `통합 정산` 클릭 시 `정산 현황`으로 이동하는지 확인 | 등록 |

## 10. 추가 회귀 테스트 케이스 - 부가서비스 통합 등록

| ID | 지적 내용 | 원인 | 검증 방법 | 상태 |
| --- | --- | --- | --- | --- |
| UAT-REG-018 | 부가서비스가 통합 POS, 골프장, 렌터카로 메뉴가 분리되어 객실 기준 등록 흐름을 확인하기 어려움 | 서비스별 목록 화면 중심으로 구성되어 객실 투숙자, 체크인 여부, 누적 이용금액, 단체 일괄 등록이 한 화면에서 연결되지 않음 | 부가서비스 메뉴는 `부가서비스 등록`, `업체/항목 관리` 2개만 보이고, 객실 카드 클릭 후 POS/골프장/렌터카를 선택해 등록해야 합니다. 단체 객실은 골프장/렌터카 선택 시 전체 객실이 기본 선택되고 개별 해제 가능해야 합니다. | 등록 |
| UAT-REG-019 | 골프장/렌터카 업체별 항목과 바우처 출력 양식이 화면 등록 흐름과 분리됨 | 업체마다 필요한 이용 항목과 티켓/바우처 출력 필드가 다르지만 예약 화면에서 고정 양식으로 처리됨 | 업체/항목 관리에서 골프장/렌터카 업체, 이용 항목, 바우처 필드를 관리하고, 부가서비스 등록 팝업에는 바우처 필드 체크박스가 없어야 합니다. 등록 후 상세 팝업의 `출력`은 업체관리에서 설정한 바우처 필드를 사용해야 합니다. | 등록 |
| UAT-REG-020 | 객실 카드에 부가서비스 세부 항목이 모두 노출되어 카드가 복잡해짐 | 목록에서 주문 상세와 요약 합계를 함께 표현함 | 객실 카드에는 `통합 POS`, `골프장`, `렌터카`, `합산` 금액만 보이고, 세부 항목/수량/상태/출력은 상세 팝업에서 확인해야 합니다. | 등록 |
| UAT-REG-021 | 부가서비스 등록 후 수락/완료 흐름을 확인할 상세 보기가 없음 | 등록 팝업과 출력 기능만 있고 주문 처리 상태 팝업이 없음 | 이용 내역이 있는 객실 클릭 시 상세 팝업이 먼저 열리고, 신규 주문은 `등록` 상태에서 `수락`, 이후 `완료`로 전환할 수 있어야 합니다. | 등록 |
| UAT-REG-022 | 체크인 예정 객실에 부가서비스 금액이 표시되어 사전 이용처럼 보임 | 객실 기준 주문을 투숙 상태와 분리하지 않고 표시함 | 체크인 예정/공실 객실은 부가서비스 합산, 세부내역, 출력 버튼이 보이지 않고 `체크인 후 등록 가능` 안내만 보여야 합니다. | 등록 |
| UAT-REG-023 | 부가서비스 상세 팝업에서 상단 요약 카드가 줄바꿈/높이 차이로 깨지고, 이용내역이 많을 때 확인이 어려움 | 객실 정보 카드와 서비스 합산 카드의 grid 기준이 다르거나 샘플 이용내역 수가 부족함 | PH01처럼 현재 투숙객 기준 이용내역 7건 이상인 객실을 열었을 때 상단 6개 객실 요약 카드와 4개 서비스 합산 카드가 같은 줄 기준으로 정렬되고, 이용내역에는 현재 투숙객 주문만 7건 표시되어야 합니다. | 등록 |
